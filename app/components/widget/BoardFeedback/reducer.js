import { } from '../../../utils/constants';

function boardFeedbackReducer(state = {}, action) {
  switch (action.type) {
    default:
      return { ...state };
  }
}

export default boardFeedbackReducer;
