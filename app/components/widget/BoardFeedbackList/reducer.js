import { } from '../../../utils/constants';

function boardFeedbackListReducer(state = {}, action) {
  switch (action.type) {
    default:
      return { ...state };
  }
}

export default boardFeedbackListReducer;
