import React from 'react';
import './style.scss';

const Footer = () => (
  <div>
    <p className="footer">
      This web app is using React, Redux, Jest, Webpack, etc ...
    </p>
  </div>
);

export default Footer;
